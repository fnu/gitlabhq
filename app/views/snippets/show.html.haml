%h3.page-title
  = @snippet.title

  - if @snippet.private?
    %span.label.label-success
      %i.icon-lock
      私有的

  .pull-right
    = link_to new_snippet_path, class: "btn btn-new btn-small", title: "新建代码片段" do
      新建代码片段


.append-bottom-20
  .pull-right
    = "##{@snippet.id}"
    %span.light
      by
      = link_to user_snippets_path(@snippet.author) do
        = image_tag avatar_icon(@snippet.author_email), class: "avatar avatar-inline s16"
        = @snippet.author_name

  .back-link
    - if @snippet.author == current_user
      = link_to user_snippets_path(current_user) do
        &larr; 我的代码片段
    - else
      = link_to snippets_path do
        &larr; 发现

%div= render 'blob'
