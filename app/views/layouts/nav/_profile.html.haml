%ul
  = nav_link(path: 'profiles#show', html_options: {class: 'home'}) do
    = link_to profile_path, title: "简介" do
      %i.icon-home
  = nav_link(controller: :accounts) do
    = link_to "账户", profile_account_path
  = nav_link(controller: :emails) do
    = link_to profile_emails_path do
      邮箱
      %span.count= current_user.emails.count + 1
  - unless current_user.ldap_user?
    = nav_link(controller: :passwords) do
      = link_to "密码", edit_profile_password_path
  = nav_link(controller: :notifications) do
    = link_to "通知", profile_notifications_path
  = nav_link(controller: :keys) do
    = link_to profile_keys_path do
      SSH 公钥
      %span.count= current_user.keys.count
  = nav_link(path: 'profiles#design') do
    = link_to "主题", design_profile_path
  = nav_link(controller: :groups) do
    = link_to "组", profile_groups_path
  = nav_link(path: 'profiles#history') do
    = link_to "历史", history_profile_path

