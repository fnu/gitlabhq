%h3.page-title 编辑组
%hr
= form_for [:admin, @group], html: { class: "form-horizontal" } do |f|
  - if @group.errors.any?
    .alert.alert-danger
      %span= @group.errors.full_messages.first
  .form-group.group_name_holder
    = f.label :name, class: 'control-label' do
      组名
    .col-sm-10
      = f.text_field :name, placeholder: "Example Group", class: "form-control"

  .form-group.group-description-holder
    = f.label :description, "详细资料", class: 'control-label'
    .col-sm-10
      = f.text_area :description, maxlength: 250, class: "form-control js-gfm-input", rows: 4

  .form-group.group_name_holder
    = f.label :path, class: 'control-label' do
      %span.cred 组的路径
    .col-sm-10
      = f.text_field :path, placeholder: "example-group", class: "form-control danger"
      %ul.cred
        %li 修改组的路径, 可能有意想不到的问题.
        %li 修改组的路径, 将会重命名这个组下的所有相关项目的路径.
        %li 修改组的路径, 组和组下面的项目, URL 都会被改变.
        %li 修改组的路径, 组下面的项目, Git 地址都会被改变.

  .form-actions
    = f.submit '保存', class: "btn btn-primary"
    = link_to  '取消', admin_groups_path, class: "btn btn-cancel"
